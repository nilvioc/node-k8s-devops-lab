apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: devops-lab

spec:
  selector:
    app: {{ .Release.Name }}

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000

  type: {{ .Values.service.type }}
